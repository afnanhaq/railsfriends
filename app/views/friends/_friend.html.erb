
  <tr id="<%= dom_id friend %>">
      <th scope="row"><%= link_to friend.first_name + " " + friend.last_name, friend %></th>
      <td><%= friend.email %></td>
      <td><%= friend.phone %></td>
      <td><%= friend.twitter %></td>
  </tr>
